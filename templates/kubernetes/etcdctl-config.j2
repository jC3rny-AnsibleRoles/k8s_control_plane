export ETCDCTL_API=3
export ETCDCTL_ENDPOINTS={% for host in ansible_play_hosts %}
https://{{ hostvars[host].ansible_fqdn }}:2379
{%- if not loop.last %},{% endif %}
{% endfor %}

export ETCDCTL_CACERT=/etc/kubernetes/pki/etcd/ca.crt
export ETCDCTL_KEY=/etc/kubernetes/pki/apiserver-etcd-client.key
export ETCDCTL_CERT=/etc/kubernetes/pki/apiserver-etcd-client.crt
export ETCDCTL_WRITE_OUT=table
